[project]
name = "agent-handoff-demo"
version = "0.1.0"
description = "Small CLI used to verify persistent project-memory handoffs"
requires-python = ">=3.11"

[project.scripts]
agent-handoff-demo = "agent_handoff_demo.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

