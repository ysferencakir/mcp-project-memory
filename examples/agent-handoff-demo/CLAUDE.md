# Claude Code instructions

Follow [AGENTS.md](AGENTS.md) as the shared project-memory and handoff protocol.
Use the `project-memory` MCP tools before and after meaningful work. This demo
assumes Claude Code and Codex work sequentially, not concurrently.

